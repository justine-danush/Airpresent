# AirPresent Native Android App

This companion app reads `TYPE_ROTATION_VECTOR` directly from Android, avoiding the browser motion-permission limitation. Open the project in Android Studio, connect an Android phone via USB with developer mode enabled, then select **Run** to install it.

The Windows receiver must already be running. Change the preset LAN address in the app if it changes.
